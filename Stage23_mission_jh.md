Stage 2：三本书抓取与归还书架
一、任务目标
机器人从小推车中依次取出三本书，通过书脊标签OCR识别每本书对应的书架层级和水平编号，然后将书放回书架指定位置。
使用的DataReplay资产：
从推车抓书：DR11.2

第三层放书：DR7.1
第四层放书：DR6.4
第五层放书：DR5.1
三本书保证分别属于第三、第四、第五层各一本，但抓取顺序由它们在推车中的左右位置决定。
二、任务初始条件
- Stage 2独立启动。
- 机器人已经上电，各控制接口可用。
- 三本书位于推车中。
- 书脊标签朝向头部相机并且没有旋转。
- 每本书标签为两行，例如：
A03
0015
解析规则：
- 第一行读取A后的两位数字，得到目标层级；
- 第二行读取四位数字，得到书本水平编号；
- 编号从人正对书架时的左边开始，向右递增；
- 默认一本书占用宽度为5 cm。
三、初始化
程序初始化：
- RGB-D同步；
- 书本检测与分割；
- 推车检测；
- OCR识别；
- 书架、层级和书位检测；
- 底盘粗导航与精定位；
- 右侧吸盘；
- DataReplay运行模块。
运行模块按照以下顺序初始化：
DR11.2
DR5.1
DR6.4
DR7.1
初始化完成后缓存四个录像episode。
每个DataReplay资产单独读取和保存：
- HDF5路径；
- 总帧数；
- 第0帧头部姿态；
- 第0帧升降柱高度；
- 第0帧双臂和手部姿态；
- D01启动或关闭事件帧；
- 对应视觉语义点在base_link中的参考位置。
D01事件帧根据各自录像动作记录确定，不沿用DR1.2的frame 20或DR2.4的frame 190。
四、首次靠近推车
如果没有指定--skip-stage2-initial-coarse：
1. 机器人恢复适合检测推车的头部和升降柱姿态。
2. 头部从多个角度检测完整推车cart_body。
3. 根据推车位置计算底盘转向角度。
4. 底盘先转向推车，再直线前进。
5. 到达推车附近后调整朝向，使机器人正对推车。
6. 底盘外沿距离推车约30 cm时结束粗导航。
7. 随后使用目标书书脊抓取点与DR11.2参考点进行精定位。
30 cm只用于进入视觉工作范围，不是最终抓取位置。
使用--skip-stage2-initial-coarse时，机器人必须已经：
- 正对推车；
- 能够观察书脊；
- 位于DR11.2精定位工作范围内。
五、单本抓取流程
以下流程循环三次。
1. 恢复DR11.2第0帧姿态
机器人从实时反馈姿态平滑恢复DR11.2第0帧中的：
- 左臂；
- 右臂；
- 头部；
- 身体升降柱；
- 左右手相关姿态。
恢复期间底盘保持零速。
第0帧全身姿态只恢复一次。后续精定位只移动底盘，对位完成后直接执行DR11.2，不再次恢复全身姿态。
2. 检测并选择目标书
1. 采集新的同步RGB-D图像。
2. 检测并分割推车中的所有可见书本。
3. 根据mask、深度、相机内参、头部姿态和升降柱高度，计算每本书在base_link中的位置。
4. 固定选择机器人视角中最右侧的可用书本。
3. OCR识别
1. 对选中书本的书脊标签区域执行OCR。
2. OCR文本框必须与选中书本mask关联。
3. 解析目标层级和水平编号。
4. 根据层级选择Place资产：
A03 → DR7.1
A04 → DR6.4
A05 → DR5.1
如果OCR无法识别，Stage 2立即停止、底盘保持零速，并输出：
OCR无法识别
如果层级不是第三、第四或第五层，Stage 2立即停止并输出：
识别结果不是第三、第四或第五层
4. 计算DR11.2抓取点
DR11.2吸取书脊，抓取点定义为：
从书脊下端向上13 cm
沿书脊宽度方向取中间位置
不使用Stage 1平放书的封面13/10规则，也不需要识别书本正反面。
5. 抓书精定位
1. 将实时书脊抓取点与DR11.2第0帧参考点比较。
2. 计算X、Y、Z误差。
3. 底盘根据X/Y误差精定位。
4. 每次底盘移动后重新检测并重关联同一本书。
5. 最多修正三轮。
验收范围：
X：±20 mm
Y：±10 mm
Z只记录，不对DR11.2升降柱轨迹进行补偿，完整使用录像原始高度。
如果三轮后仍未达到范围，不执行DR11.2，停止任务并报告最终误差。
6. 执行DR11.2
1. 以录像原始速度完整执行DR11.2。
2. 在DR11.2对应事件帧异步启动右侧吸盘。
3. 吸盘事件不阻塞后续DataReplay帧。
4. 完成全部录像帧，使机器人吸取并抬起书本。
当前不调用holding检查，Replayer只保留可选检查接口。
六、从推车前往书架
1. 保持吸盘开启。
2. 底盘向后移动20 cm离开推车。
3. 头部依次在以下角度扫描书架：
-45°、-30°、-15°、0°、15°、30°、45°
4. 如果头部扫描没有检测到书架：
   - 底盘先向右每次旋转15°并检测，最多累计180°；
   - 仍未检测到时，底盘回到搜索初始朝向；
   - 再向左每次旋转15°并检测，最多累计180°。
5. 检测到书架后，根据书架三维位置完成底盘转向和直线粗导航。
6. 底盘外沿距离书架约45 cm时结束粗导航。
7. 调整底盘朝向，使机器人面向书架。
8. 最终位置由目标书位与对应Place DataReplay第0帧参考确定。
当前任务只实现机器人位于推车正面的路线，不实现推车背面路线或预留背面路线接口。
七、确定目标放书位置
机器人根据OCR得到的层级和编号，在目标层中扫描已有书本标签。
检测到目标编号前后相邻书
例如目标为0015，检测到0014和0016：
1. 找到0014靠近空位的内侧边缘。
2. 找到0016靠近空位的内侧边缘。
3. 分别计算两条内侧边缘的三维点。
4. 取两个三维点的中点作为0015目标位置。
5. 两条书脊排列方向用于计算书架水平轴和yaw。
6. 两条内侧边缘之间的区域作为目标放置框。
检测到两本非相邻编号书
例如检测到0012和0018：
1. 根据两本书的三维位置和编号差计算现场实际编号间距。
2. 沿两本书的三维排列方向插值计算目标编号位置。
3. 根据两本书排列方向计算书架yaw。
只检测到一本编号书
1. 使用书架整体方向确定书架水平轴和yaw。
2. 根据已识别书本编号和目标编号计算编号差。
3. 每相差一个编号，沿书架编号增长方向移动5 cm。
4. 计算目标位置。
5. 目标放置框的默认宽度为5 cm。
编号增长方向固定为人正对书架时从左向右。
八、书架放书精定位
1. 选择Place资产
第三层 → DR7.1
第四层 → DR6.4
第五层 → DR5.1
2. 恢复观察姿态
精定位前只恢复所选Place资产第0帧中的：
- 头部姿态；
- 身体升降柱高度。
右臂继续保持当前吸书状态，不在此时恢复完整Place手臂姿态。
3. 计算对位误差
使用与对应Place第0帧参考相同的放置语义点，计算：
- 前后误差；
- 左右误差；
- yaw误差；
- Z只用于记录。
底盘最多进行三轮修正，每轮移动后重新检测书架、OCR标签和目标放置框。
验收范围：
前后：±30 mm
左右：±20 mm
yaw：±5°
如果最终未达到范围，不执行Place DataReplay。
4. 执行Place
1. 对位成功后，恢复所选Place资产第0帧完整全身姿态。
2. 吸盘保持开启。
3. 以原始速度完整执行DR5.1、DR6.4或DR7.1。
4. 在该资产对应事件帧关闭右侧吸盘。
5. 不使用吹气。
6. 完成剩余回放，使右臂和身体离开书架。
7. 回放结束后确认右侧吸盘已经释放书本。
九、返回推车
第一、第二本放置完成后：
1. 底盘向后移动45 cm离开书架。
2. 使用上一次推车位置确定初始扫描方向。
3. 头部先向机器人右侧转动30°检测推车，底盘保持不动。
4. 根据新的cart_body检测结果计算推车方向。
5. 底盘先转向推车，再直线移动。
6. 到达推车附近后调整朝向，面向推车。
7. 每轮必须使用新的cart_body检测结果，上一次位置只作为扫描提示。
8. 使用当前最右侧书脊抓取点恢复DR11.2第0帧相对位置。
9. 开始下一轮抓书。
推车在任务期间保持静止。
十、Stage 2结束及Stage 3衔接
第三本Place完成后：
1. 不再返回推车。
2. 底盘先向后移动20 cm。
3. 不关闭Stage 3仍需复用的Place DataReplay运行模块。
4. 直接进入Stage 3。
5. Stage 3开始后，将机器人移动到“底盘外沿距离书架正面60 cm”的扫描位置。
Stage 3：错放书检测、取出并归位
一、任务目标
Stage 3直接接在Stage 2后执行。
机器人在书架前扫描第三、第四和第五层所有可见书本的OCR标签，找出一本放错位置的书，将其从错误位置取出，再放回标签指定的正确位置。
按当前任务描述，书架中保证恰好有一本放错的书，并且它的正确目标位置为空。
错误包括：
- 层级错误：标签目标层与实际所在层不同；
- 编号位置错误：实际水平位置与标签编号要求的位置不一致。
二、使用的DataReplay资产
从书架取书：
第三层取书：DR9.2
第四层取书：DR8.1
第五层取书：DR10.1
向书架放书：
第三层放书：DR7.1
第四层放书：DR6.4
第五层放书：DR5.1
Stage 3顺序初始化并缓存：
DR9.2
DR8.1
DR10.1
Stage 2已经加载的DR7.1、DR6.4和DR5.1继续复用。
每个Pick资产单独读取：
- HDF5路径；
- 总帧数；
- 第0帧全身姿态；
- 抓取视觉参考；
- 吸盘启动事件帧；
- 原始回放速度。
三、进入书架扫描位置
1. 从Stage 2第三本Place结束位置继续。
2. 根据书架位置调整底盘前后距离。
3. 机器人面向书架。
4. 底盘外沿距离书架正面保持60 cm。
5. 进入全书架OCR扫描。
四、扫描第三、第四和第五层
1. 机器人依次使用能够观察第三、第四、第五层的头部和升降柱姿态。
2. 每个层级都执行头部角度扫描：
-45°、-30°、-15°、0°、15°、30°、45°
3. 每个角度采集新的同步RGB-D图像。
4. 检测并分割画面中的书本。
5. OCR读取每本书的两行标签。
6. OCR结果必须与对应书本mask关联。
7. 将不同角度重复检测到的同一本书进行合并。
8. 为每本书记录：
   - OCR层级；
   - OCR编号；
   - 实际所在层；
   - 当前三维位置；
   - 书脊方向；
   - 检测置信度。
五、判断错放书
层级错误
如果：
OCR目标层级 != 实际所在层级
则该书为层级放错。
编号位置错误
如果层级正确，则检查它在该层中的排列位置：
- 根据其他OCR书本的编号和三维位置建立编号增长方向；
- 编号从人正对书架时的左侧向右递增；
- 检查该书当前位置是否符合其编号顺序；
- 使用相邻书实际间距，或者单本情况下的默认5 cm间距，推算正确位置。
如果实际位置与正确位置不一致，则该书为编号放错。
任务场景保证只存在一本错放书。
如果没有找到错放书、找到多本错放书，或者OCR结果不足以唯一确定错放书，停止Stage 3并报告识别结果。
六、选择取书DataReplay
根据错放书当前实际所在层选择Pick资产：
实际在第三层 → DR9.2
实际在第四层 → DR8.1
实际在第五层 → DR10.1
注意：Pick资产由当前实际层决定，而不是OCR标签中的目标层决定。
七、错放书抓取精定位
1. 保持对错放书的目标锁定。
2. 导航到对应Pick DataReplay的视觉工作范围。
3. 从当前实时姿态平滑恢复所选Pick资产第0帧完整全身姿态。
4. 恢复期间底盘保持零速。
5. 检测错放书，并计算与该Pick资产第0帧相同语义的抓取点。
6. 将实时抓取点与第0帧参考点比较。
7. 底盘根据X/Y误差精定位。
8. 每次移动后重新检测并重关联同一本书。
9. 最多修正三轮。
验收范围：
X：±20 mm
Y：±10 mm
Z只记录，不修改DataReplay原始升降柱轨迹。
八、执行书架Pick
1. 对位成功后直接完整执行选定的DR8.1、DR9.2或DR10.1。
2. 不重复恢复第0帧姿态。
3. 在对应资产的吸盘事件帧异步启动右侧吸盘。
4. 完成全部回放帧，使机器人将错放书从书架取出并抬起。
5. 当前不调用holding检查，保留可选检查接口。
九、计算正确归还位置
根据错放书OCR标签确定：
- 正确目标层级；
- 正确水平编号。
目标位置采用与Stage 2相同的计算规则：
- 有前后相邻编号书时，取两书内侧边缘的三维中点；
- 有两本非相邻编号书时，根据编号差和三维位置插值；
- 只有一本编号书时，使用书架整体方向和每编号5 cm间距推算；
- 使用书架整体方向或多本书脊排列方向计算yaw。
十、移动到正确放书位置
1. 保持右侧吸盘开启。
2. 根据目标层级和水平位置移动底盘。
3. 进入对应Place DataReplay的视觉工作范围。
4. 机器人继续面向书架。
5. 不返回60 cm扫描位置，也不执行无关任务。
6. 直接进入目标位置精定位。
这里的“Pick和Place连续执行”表示两者属于同一个连续任务，但不能跳过目标位置视觉精定位和Place第0帧姿态恢复。
十一、正确位置精定位与Place
1. 根据目标层级选择Place资产：
第三层 → DR7.1
第四层 → DR6.4
第五层 → DR5.1
2. 精定位前只恢复对应Place资产第0帧的头部和升降柱观察姿态。
3. 保持吸盘开启和书本抬起状态。
4. 重新检测目标层和目标放置框。
5. 计算前后、左右和yaw误差。
6. 底盘最多修正三轮。
验收范围：
前后：±30 mm
左右：±20 mm
yaw：±5°
7. 对位成功后恢复对应Place资产第0帧完整全身姿态。
8. 以原始速度完整执行对应Place DataReplay。
9. 在该资产的关闭事件帧关闭右侧吸盘。
10. 不使用吹气。
11. 回放结束后检查书本已经释放。
十二、Stage 3结束
错放书归位完成后：
1. 底盘向后移动，恢复到面向书架的位置。
2. 使用书架视觉结果调整最终前后距离。
3. 最终保持底盘外沿距离书架正面60 cm。
4. 机器人保持面向书架。
5. 不再扫描推车，不返回终点。
6. 关闭所有Pick和Place DataReplay运行模块。
7. Stage 3结束。
最终主线为：
Stage 2第三本放书完成
→ 进入书架前60 cm扫描位置
→ 扫描第三、第四、第五层全部OCR
→ 找到唯一错放书
→ 根据实际所在层选择DR8.1/DR9.2/DR10.1
→ 对准并执行书架Pick
→ 根据标签确定正确层级和编号
→ 移动并对准正确书位
→ 执行DR5.1/DR6.4/DR7.1
→ 回到书架前60 cm
→ 结束