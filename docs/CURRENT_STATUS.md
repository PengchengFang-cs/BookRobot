# FPC 当前状态

更新时间：2026-09-04

## 当前阶段

Stage 1 已有三本完整真机成功基线；当前 `feature/stage23-integration` 又完成了两本速度实验所需的连续扫描、导航提速、单帧精定位、DataReplay 预加载和计时记录。2026-09-03 的两本完整实验仍约为 5 分 43 秒（不含第一次到书桌），尚未达到 5 分钟目标。Stage 2 可以从本分支继续推进，接口和缺口以 [`HANDOFF_STAGE23_20260823.md`](HANDOFF_STAGE23_20260823.md) 为准。

## 2026-09-03 Stage 1 交接快照

- 当前开发仓库：`/home/cvailab/Ruan/Library_306`；当前分支：`feature/stage23-integration`。
- `395e211`～`7ab2246` 是尚待推送的 Stage 1 优化提交：降低精定位等待、加速导航、修正推车 Place 前后参考、调整槽位/容差，并撤销会卡住视觉服务的并行推理，改为 `45°` 成功优先、失败再处理 `60°`。
- 当前两本实验会在第一本开始前把 Pick 1.2 和 Place 2.4 各加载一次到内存；普通书本和推车精定位每轮均只使用一张成功图。
- 当前速度：普通粗导航直行 `0.5 m/s`，书桌到推车的长距离直行 `0.15 m/s`，粗导航旋转 `0.36 rad/s`，精定位旋转 `0.24 rad/s`。
- 当前 Pick 验收：前后 `±30 mm`、左右 `±20 mm`；最多三次修正。当前 Place 验收：前后通常 `±20 mm`，最后一次允许 `±30 mm`，左右 `±20 mm`，yaw `±5°`。
- 当前推车槽位中心距左边缘为 `0.20/0.35/0.50 m`；两本速度实验使用前两个槽位。
- `20260903_125926` 完整两本耗时约 `373.8 s`，排除第一次到书桌约 `342.8 s`，其中两本核心约 `335.1 s`。两本均完成，但仍超过 5 分钟目标。
- `20260903_213002` 跳过首次粗导航后，第 1 本 Pick 一次修正达标（前后 `12.3 mm`、左右 `3.5 mm`，精定位 `27.0 s`），Place 一次修正达标（前后 `5.9 mm`、左右 `12.9 mm`、yaw `0.97°`）；第一本 Pick→Place 软件流程耗时 `138.1 s`。现场观察到放书实际偏差超过十厘米并掉落，随后流程在返回书桌途中按用户要求停止。
- 上述放书实验使用过“参考前沿向机器人侧调整 `30 mm`”的改动。现场确认该补偿过度，2026-09-04 已把三个平台参考 X 恢复到修改前的 `0.800194/0.945166/0.942743 m`；该恢复尚未重新做真机实验。
- 每次实验的原图、叠加图、终端记录和分段计时保存在时间戳目录 `logs/record_rebot/<run_id>/`，这些实验产物不进入 Git。

## 已完成且仍有效

- `/home/cvailab/fpc` 保留为已验证基线仓库；`/home/cvailab/Ruan/Library_306` 是可独立运行的多人最终集成仓库。两边的 `scripts/deploy_to_robot.sh` 都只部署自身已提交且干净的HEAD；Wanda 的 `/home/unix_ai/fpc` 是普通运行副本，GitHub 只作备份。
- 视觉链路为 Wanda RGB-D → 5090 Grounding DINO/SAM 二维 mask → Wanda 本地深度/姿态三维几何。候选置信度阈值为 `0.25`，最多返回置信度最高且几何有效的 5 本，不足 5 本返回实际数量。
- 桌面书抓取点使用机器人视角下从长轴近端向内 `0.13 m`、从右边缘向左 `0.10 m` 的规则。
- 新 Pick 的 frame 0 13/10 规则点为 `base_link=(0.8981937,-0.2520129,0.7568440) m`；frame 158 蓝色吸盘接触端投影 Y 为 `-0.2589220 m`。最终运行参考组合为 `(0.8981937,-0.2589220,0.7568440) m`，即 X/Z 保留 frame 0 规则点，只用真实接触修正 Y。
- 默认 `--book-pick` 不做 `0.48 m` 粗定位；只有显式 `--book-coarse` 才启用粗定位。`--check` 默认关闭，且不能与 `--book-pick` 同时使用。
- 精确对位只驱动底盘 X/Y；视觉 Z 只作诊断，Pick 高度完全复现 DataReplay。当前回放门限为 X 前后 `±30 mm`、Y 左右 `±20 mm`；任一超差都不会启动 DataReplay。
- 按用户要求，目标选择和重关联使用最近视觉规则点，不以书本尺寸、长轴角或预测距离拒绝候选；这些量后续只作为诊断记录。
- 新 Pick HDF5 的 333 帧会完整保留：底盘速度、双臂、头部、升降柱、左夹爪和右灵巧手均按录制数据发布；底盘前进约 268 mm、回退约 278 mm。
- 正式第 0 帧前，从实时关节反馈以有界小步平滑恢复双臂、头部和升降柱姿态；HDF5 若记录了左夹爪或右灵巧手，也会发送其第 0 帧录制值。预置期间底盘命令固定为零，并在进入正式回放前核对双臂、头部和升降柱反馈。
- collector 不记录 D01；用户确认新录制在开始约 1 秒后打开吸盘，因此 `right_suction_start` 在 frame 20 发布后由独立线程启动，等待 holding 不阻塞 frame 21–332。
- 小推车粗导航固定为：Pick 后退 `0.20 m`、连续左转到 `90°`，运动中只拍 `45/60°` 两帧，旋转结束后先处理 `45°`、无结果再处理 `60°`；随后以 `0.15 m/s` 直行到车旁、右转 `90°`、前进 `0.20 m`。整车中心只估计中间的长距离直行，不再执行 `0.8 m` 车体中心微调。
- 正面 RGB-D 已从整车 mask 中提取正确的上层托板；平台物理宽度按 `75 cm` 使用。当前实验槽位中心距左边缘为 `20/35/50 cm`。
- 旧地图导航、Place 最后分段续跑、`0.8 m` 精调以及“粗导航后立即 Place”的编排已从当前源码移除。
- Place 2.4 第 0 帧参考已标定：升降柱约 `0.20 m`、托板宽 `0.7665 m`、第一槽距左边缘 `0.17 m`。精定位分别使用托板前沿、槽位和方向计算前后、左右和 yaw；每轮使用第一张检测成功的图，失败才重拍，最多修正三次。
- Place 不追求毫米级精确复现；当前验收范围为前后通常 `±20 mm`、最后一次 `±30 mm`、左右 `±20 mm`、yaw `±5°`。
- `--book-place`、`--book-pick-place` 已接通。为缩短逐项速度优化实验，当前 `--stage1-loop` 固定执行两本书、使用槽位 1/2，并只在第一本 Place 后按后退 `0.20 m`→右转→直行→左转→前进 `0.20 m` 返回书桌。最终比赛版本仍需恢复五本。
- 两本循环会记录部署提交和 `timings.jsonl` 分段耗时。每项优化单独提交；只有动作、精度、回放帧数和 D01 门禁均不退化且核心耗时明确下降，才保留该提交，否则使用 `git revert` 回滚。
- 当前两本循环、计时和速度改动已多次部署并真机运行；最新提交与本页记录将推送到 GitHub 的 `feature/stage23-integration`，不合并到 `main`。
- Stage 2→3连续任务的fail-closed编排已在`feature/stage23-integration`融合并同步到5090的`/home/cvailab/Ruan/Library_306`：`5141b82`使部署源仓库自包含，`d8ae9f2`新增任务代码和交接，`a49d6c1`补5090 Python 3.8兼容。当前仍被D01事件帧、录像参考、OCR、竖直书脊/书架感知、粗导航和新资产合同显式阻断；本轮不做真机实验。

## 当前问题

- Stage 1 两本核心耗时当前约 `335 s`，仍高于“不含第一次粗导航、两本五分钟内”的目标。
- 2026-09-03 最新一次 Place 的软件残差在厘米内，但现场落点偏差超过十厘米并掉落；已按用户结论撤销过度的 `30 mm` 平台前后补偿，尚未重新验证。

- [`BOT-20260820-12`](ROBOT_ISSUES.md#bot-20260820-12)：推车粗导航曾在旋转后重复交换 X/Y，导致错误驶向并撞到书桌；公式已恢复为固定书本坐标系，等待真机复测。

- [`BOT-20260818-03`](ROBOT_ISSUES.md#bot-20260818-03)：精确旋转仍需基于 odom 反馈验证。
- [`BOT-20260818-09`](ROBOT_ISSUES.md#bot-20260818-09)：最新一次 vector 对位最终为 `dx=-8 mm, dy=-9 mm`，满足当前 X/Y 范围但不满足历史 4 mm 目标；该残差也会直接平移实际吸附点。
- [`BOT-20260818-10`](ROBOT_ISSUES.md#bot-20260818-10)：旧 Pick 的底盘轨迹和第 0 帧姿态修复已部署并越过原失败点，书已实际吸起；但 D01 stale/fault 使程序退出，仍缺全 607 帧和 holding ACK 证据。
- [`BOT-20260819-11`](ROBOT_ISSUES.md#bot-20260819-11)：完整 Pick 已抓起书且 Z 正确，但实际点相对中心 X 约偏 50 mm、Y 约偏 10–20 mm；13/10 非中心目标、未标定的录制接触偏移和回放内开环底盘位移需要分离测量。

## 下一步

1. Stage 2 从 [`HANDOFF_STAGE23_20260823.md`](HANDOFF_STAGE23_20260823.md) 的现有入口、资产表和待补接口继续，避免重新实现 Stage 1 已有的视觉、对位与 DataReplay 桥。
2. 回到 Stage 1 时，先用恢复后的 Place 参考复测单本实际落点，再继续两本五分钟目标；不要重新加入已确认过度的 `30 mm` 补偿。
3. 两本节拍稳定后再决定最终比赛使用两本、三本或恢复更多槽位。

## 本次验证边界

- 新 HDF5 已只读核对为 333 帧、16.6 秒，六个控制通道形状正确；底盘角速度全程为零。
- frame 158 目标书被吸盘遮挡，因此接触标定不使用邻书 mask；蓝色吸盘远端像素 `(146.5,183.0)` 投影 Y 与 frame 0 同一本书规则 Y 相差约 6.9 mm。
- Place 2.4 参考标定只读处理 HDF5，并使用 5090 mask；本次实现与验证没有发送底盘、机械臂、升降柱或 D01 动作。

## 更新要求

- 完成部署、真机实验或问题调查后更新本页；历史过程写入 [`WORKLOG.md`](WORKLOG.md)。
- 当前问题解决后从本页移除，但在 [`ROBOT_ISSUES.md`](ROBOT_ISSUES.md) 中保留原编号和验证结果。
- 只把现场观察或可复现测试支持的内容写成事实；不记录密码、密钥、令牌、证书内容、数据集、模型或日志原文件。
