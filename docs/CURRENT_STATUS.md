# FPC 当前状态

更新时间：2026-08-18

## 当前阶段

当前处于书本感知真机验证与底盘转向接近验证阶段。书本感知替换的代码已经完成，但尚未用真实书本场景验证 mask 和三维吸取点；在该验证完成前，不接入导航或吸取动作。

## 已完成且仍有效

- FruitTest 已复制到受控的 `fpc` 工作区；当前开发使用该可废弃副本，不修改机器人上的其他旧工程。
- 书本感知已经替换原有 HSV 水果识别路径。5090 mask client、Wanda 本地 RGB-D 几何、同步帧选择、以采集时刻为基准的 deadline、仅感知入口、测试及 review fixes 均已进入 `main`。详细设计与实施记录见[书本感知替换设计](superpowers/specs/2026-08-17-fruittest-book-perception-design.md)和[实施计划](superpowers/plans/2026-08-17-fruittest-book-perception.md)。
- 部署路径是从 5090 的工作区经 SSH 更新 Wanda，不由 Wanda 从 GitHub pull。
- 底盘释放后，用户已在真机上确认前进、后退和升降可用。

## 正在进行

- 采集真实书本场景，核验二维 mask 与 Wanda 本地计算的三维吸取点。
- 隔离手柄旋转输入，并使用精确 odom 反馈验证底盘旋转。

## 当前问题

- [`BOT-20260818-02`](ROBOT_ISSUES.md#bot-20260818-02)：手柄旋转输入仍需隔离和验证。
- [`BOT-20260818-03`](ROBOT_ISSUES.md#bot-20260818-03)：精确旋转仍需基于 odom 反馈验证。

以上条目只记录已观察到的问题，不把未验证的原因写成结论。

## 下一步

1. 采集包含真实书本的同步 RGB-D 场景。
2. 对同一次 capture 保存 mask 叠加图和感知 JSON：肉眼确认 mask 覆盖目标书本，确认 JSON 的 `frame_id` 为 `base_link`，并核对吸取点符合沿书本长轴从近端向内 `0.13 m`、沿短轴从右边缘向左 `0.10 m` 的固定偏移规则。
3. 只有上述视觉与几何验证通过后，才规划接入导航或吸取动作。

## 更新要求

- 完成实现、部署、真机实验或问题调查后，更新日期和仍然有效的状态；历史过程写入 [`WORKLOG.md`](WORKLOG.md)。
- 当前问题解决后，从本页移除，但在 [`ROBOT_ISSUES.md`](ROBOT_ISSUES.md) 中保留原编号并更新状态。
- 只把现场观察、命令输出或按测试设计执行并保留结果的测试所支持的内容写成事实；未验证根因必须明确标为假设。
- 不在文档中记录密码、密钥、令牌、证书内容或其他凭证。
