# FPC 当前状态

更新时间：2026-08-18

## 当前阶段

书本导航抓取位对准已完成第一次真机闭环。真实场景返回 5 本书的 mask 和 `base_link` 吸取点，流程选择距离 Stage 1 DataReplay 固定抓取位置最近的一本，完成底盘横向与前后对位并重新检测；本阶段仍未运行机械臂、DataReplay 或吸盘。

## 已完成且仍有效

- FruitTest 已复制到受控的 `fpc` 工作区；当前开发使用该可废弃副本，不修改机器人上的其他旧工程。
- 书本感知已经替换原有 HSV 水果识别路径。5090 mask client、Wanda 本地 RGB-D 几何、同步帧选择、以采集时刻为基准的 deadline、仅感知入口、测试及 review fixes 均已进入 `main`。详细设计与实施记录见[书本感知替换设计](superpowers/specs/2026-08-17-fruittest-book-perception-design.md)和[实施计划](superpowers/plans/2026-08-17-fruittest-book-perception.md)。
- 部署路径是从 5090 的工作区经 SSH 更新 Wanda，不由 Wanda 从 GitHub pull。
- 底盘释放后，用户已在真机上确认前进、后退和升降可用。
- 用户确认此前手柄不能旋转是操作方式错误；改用正确操作后已能旋转并把机器人移动到测试位置。
- 真实场景临时联调已跑通“Wanda RGB-D → 5090 mask → Wanda 本地几何”。头部保持标定低头姿态 `joint_head1=0.24997 rad` 时，桌上 5 本书全部进入画面。FruitTest 现按 `0.25` 阈值过滤并返回最多 5 本几何有效书；2026-08-18 真机复测返回 5 本，置信度为 `0.538–0.565`，调试图分别覆盖五本书。
- 单仓库直接部署已经启用：`/home/cvailab/fpc/scripts/deploy_to_robot.sh` 从本地干净的 `main` 导出已提交文件并直接同步到 Wanda 普通运行副本，不访问 GitHub、不要求机器人有 Git，也不删除机器人额外文件。首次部署版本为 `104e9278761988cffe04be3200e1d438564789ea`；Wanda 端脚本语法、Python 编译、10 项对位相关纯测试、`--book-align` 命令行入口和部署版本标记均验证通过，未产生运动。
- `--book-align` 已改为不执行底盘 release、不等待释放按键，并在书本检测完成后才加载旧导航运行时。RGB-D 使用可靠订阅，视觉入口加载现有 7443 gRPC 环境；同步器保留 512 条关节历史并接受实测时间偏差。
- 2026-08-18 第一次完整真机对位成功退出。初始残差为 `dx=+0.114 m, dy=+0.048 m, dz=+0.008 m`，执行 4 条横向/前后命令后，最终残差为 `dx=-0.011 m, dy=-0.026 m, dz=+0.009 m`。最终叠加图覆盖 5 本书；本次没有调用机械臂、DataReplay 或吸盘。
- 高度方案已按旧 Pipeline 的新补丁重新分工：FPC 选书只比较 X/Y，底盘导航强制令 observed Z 等于 reference Z，因此不再生成 torso 命令；初始视觉只计算一次 `z_offset_m=observed_z-reference_z` 并交给后续 Pipeline。此前部署的导航阶段 `0.20 m + delta` 方案已被此设计取代，历史仍保留在工作日志和问题档案中。

## 正在进行

- 一次性书本导航对位入口已完成第一次真机闭环；XY/Z 分流已部署到 Wanda，FPC 本地 55 项测试和机器人端 15 项非运动测试通过。后续真机对位只验证 X/Y，Z offset 由尚未部署的旧 Pipeline token/DataReplay 补丁消费。
- D01 服务运行正常，但右吸盘 Modbus 当前只能写请求、收不到任何回包；软件侧已经排除设备节点、权限、端口占用、地址和波特率，等待现场检查供电和 RS485 物理链路。

## 当前问题

- [`BOT-20260818-03`](ROBOT_ISSUES.md#bot-20260818-03)：精确旋转仍需基于 odom 反馈验证。
- [`BOT-20260818-08`](ROBOT_ISSUES.md#bot-20260818-08)：右 D01 吸盘串口请求无回包，等待现场检查吸盘主电源、指示灯和 RS485 接线。

以上条目只记录已观察到的问题，不把未验证的原因写成结论。

## 下一步

1. 等旧 Pipeline 的 Z token/DataReplay 内存偏移补丁进入可部署位置后，对接 FPC 输出的固定 `z_offset_m`。
2. 后续真机对位继续保存 X/Y 移动前后残差；Z 精度在完整 Pick/Place torso 偏移链路中单独验收。

## 更新要求

- 完成实现、部署、真机实验或问题调查后，更新日期和仍然有效的状态；历史过程写入 [`WORKLOG.md`](WORKLOG.md)。
- 当前问题解决后，从本页移除，但在 [`ROBOT_ISSUES.md`](ROBOT_ISSUES.md) 中保留原编号并更新状态。
- 只把现场观察、命令输出或按测试设计执行并保留结果的测试所支持的内容写成事实；未验证根因必须明确标为假设。
- 不在文档中记录密码、密钥、令牌、证书内容或其他凭证。
