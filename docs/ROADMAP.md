# FPC 路线图

更新时间：2026-08-18

路线图只记录阶段顺序、状态和完成证据。具体方案与任务拆分保留在对应的设计和实施计划中。

各阶段如需判断数值误差或按次数验证，必须在对应阶段的测试设计中写明容差、重复次数和记录字段，并在真机执行前确定；路线图不预设尚未设计和验证的阈值。

## 阶段 1：书本感知集成

**状态：** 代码完成；真实书本视觉与几何验证待做。

目标是由 5090 产生书本二维 mask，由 Wanda 使用本地同步深度、相机内参和机器人姿态恢复 `base_link` 下的三维吸取点。

**已有证据：** 书本感知已替换 FruitTest 的 HSV 水果路径；5090 mask client、Wanda 本地 RGB-D 几何、同步帧选择、capture-bound deadline、仅感知入口、自动化测试和 review fixes 已进入 `main`。

**完成证据：** 对同一次真实书本 capture 保存 mask 调试叠加图和 perception-only JSON；叠加图显示 mask 覆盖目标书本，JSON 记录 `"frame_id": "base_link"` 和吸取点。调试叠加图或配套几何记录包含长短轴、机器人视角近端、右边缘和吸取点投影，并记录由这些量推导的 long/right inset，以分别核对 `0.13 m`/`0.10 m` 固定偏移；仅感知测试入口没有创建导航或机械臂对象。当前 perception-only JSON 报告 `ok`、`frame_id` 和 `suction_point_m`，不含所需几何字段；这些字段属于本阶段待补充的诊断输出，不作为已有能力。

详细方案见[书本感知替换设计](superpowers/specs/2026-08-17-fruittest-book-perception-design.md)和[实施计划](superpowers/plans/2026-08-17-fruittest-book-perception.md)。

## 阶段 2：底盘转向接近

**状态：** 进行中。

隔离旋转输入，采用精确 odom 反馈验证转向，再验证朝目标接近；不把未验证的输入或控制根因作为结论。

**已有证据：** 底盘释放后，用户已在真机上确认前进、后退和正确手柄操作下的旋转可用。2026-08-18 一次书本对位从 `dx=+0.114 m, dy=+0.048 m` 修正到 `dx=-0.011 m, dy=-0.026 m`，完整流程成功退出；升降位于 `0.280 m` 上限，约 9 mm 高度残差未消除。待处理问题见 [`BOT-20260818-03`](ROBOT_ISSUES.md#bot-20260818-03) 与 [`BOT-20260818-07`](ROBOT_ISSUES.md#bot-20260818-07)。

**完成证据：** 每次测试记录目标角、odom 起止姿态、由二者计算的实际转角、转角误差、旋转方向、接近目标距离、odom 起止位置、实际平移距离、终点误差、最终朝向和用户现场观察；是否通过按真机执行前写入本阶段测试设计的容差与重复次数判断。

## 阶段 3：吸书动作

**状态：** 待开始。

用书本与吸盘的真实几何关系替换 FruitTest 中面向水果的距离和固定动作参数。

**完成证据：** 每次测试保存目标吸取点、机械臂到达结果、吸盘接触结果、吸附结果、抬起结果、撤离结果，以及当次使用的书本/吸盘几何和动作参数；是否通过按真机执行前写入本阶段测试设计的容差与重复次数判断。

## 阶段 4：端到端 demo

**状态：** 待开始。

在可废弃 demo 中串联书本感知、底盘、机械臂与吸取流程。

**完成证据：** 每次运行记录从请求到结束的场景采集、书本定位、底盘接近、机械臂到达、吸附、抬起、撤离和结束结果，且流程中无需人工插入控制命令；完成所需次数在真机执行前写入本阶段测试设计。

## 阶段 5：比赛架构集成

**状态：** 待开始。

只把已按对应阶段测试设计完成规定次数并保留记录的部件迁移到实际比赛任务架构，不整体搬运尚未验证的 demo 假设。

**完成证据：** 每个迁移部件均保存部件级回归记录，并保存完整任务的比赛架构集成记录；记录包含测试设计预先规定的输入、步骤结果、最终结果和执行次数。
