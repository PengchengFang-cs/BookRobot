# 机器人故障档案

本文件保存带稳定编号的机器人问题历史。历史条目永不删除；问题变化时更新状态、最近更新时间、验证结果和剩余工作，不复用编号，也不把绕过方案写成永久修复。

## 状态定义

- **待处理**：问题已记录，但尚未开始系统调查。
- **调查中**：正在收集证据或隔离故障边界，根因尚未确认。
- **已绕过**：已有可恢复功能或继续受控测试的临时方法，但永久修复尚未确认。
- **已解决**：已实施永久修复，并按记录的验证方法确认问题消失。
- **无法复现**：在明确记录的环境和步骤下未能再次观察到问题；这不等于已解决。

所有原因判断都必须区分确认事实与推测。没有直接证据支持的原因必须明确标为**假设**，不得写成已确认根因。

## BOT-20260818-01

- **标题**：重启后底盘不响应
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已解决（FPC 导航职责）
- **现象与影响**：机器人重启后，网络、ROS 和 controller 健康，但底盘不响应移动控制；当时无法用底盘继续真机测试。
- **复现/上下文**：发生在机器人重启后的释放流程中。项目路径 `/home/unix_ai/fpc/release_base.sh` 通过 `fruit_movebase_mode_controller` 的 `movebase_mode_command/mode_command` 依次发送 `5` 和 `0`。
- **确认事实与证据**：
  - 重启后网络可达，ROS 与 controller 状态健康，但底盘不响应。
  - `movebase_mode.yaml` 存在。
  - 用户在机身执行释放/旋转释放，并重启机器人页面后，已现场确认前进和后退恢复可用。
- **根因或假设**：确切硬件 mode 语义没有可用文档，当前证据不足以确认更深层根因。不得把 `5`、`0` 或机身操作分别解释为某个确定硬件状态。可能存在重启后 mode transition 或释放状态未完成的情况，但这仅是**假设**。
- **处理**：执行现有 `release_base.sh` 流程；用户随后在机身执行释放/旋转释放并重启机器人页面，以恢复底盘移动。该处理是现场绕过方案，不是永久修复。
- **验证**：用户现场操作后确认机器人可前进、后退；网络、ROS 和 controller 保持健康。
- **剩余工作/链接**：查找或补充硬件 mode 的权威语义文档；设计可重复的重启后状态检查，区分脚本 mode transition、机身释放和机器人页面重启各自的作用。相关当前状态见 [`CURRENT_STATUS.md`](CURRENT_STATUS.md)。

## BOT-20260818-02

- **标题**：手柄旋转未产生 `angular.z`
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已解决
- **现象与影响**：前进、后退和升降可用，但用户进行的左右旋转尝试均未使底盘旋转；当前不能依赖手柄执行转向。
- **复现/上下文**：在底盘释放并恢复平移后，用户分别尝试左右旋转，同时监控 `/cmd_vel`。
- **确认事实与证据**：
  - 左右旋转尝试期间，`/cmd_vel` 的 `angular.z` 始终为 `0`。
  - 平移操作期间，`/cmd_vel` 的 `linear.x` 会变化。
  - 对已编译 teleoperation 映射的只读检查表明，差速 DD 模式下右摇杆水平轴预期产生角速度。
  - 第二次监测时，用户的操作产生了最高约 `0.399 m/s` 的 `linear.x`，`angular.z` 仍为 `0`；这与操作了平移轴一致。
  - 用户随后确认此前操作方式错误，改用正确操作后已能旋转并将机器人移动到需要的测试位置。
- **根因或假设**：根因已确认是手柄操作方式错误，不是 teleoperation 映射或底盘旋转执行故障。
- **处理**：改用正确的旋转操作；未修改软件。
- **验证**：用户现场确认旋转恢复可用，并完成机器人位置调整。
- **剩余工作/链接**：无需为本条修改代码；如果以后再次出现“正确操作但不旋转”，应创建新问题或以新证据重新打开本条。历史过程见 [`WORKLOG.md`](WORKLOG.md)。

## BOT-20260818-03

- **标题**：组合释放与 `30°` spin 后总航向约 `57.8°`
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已绕过
- **现象与影响**：用户授权在空旷环境中左转 `30°`；连续执行 release 与 spin 的整个组合序列后，最终 odom 总航向约为 `57.8°`，而 `/spin` 自身末反馈约为 `32.9°`。由于缺少 release 后、spin 开始前的独立 odom 基线，约 `24.9°` 仅是尚未被 spin 反馈解释的残差观察，不能据此认定 `/spin` 本身不一致或把残差归入某个执行阶段；当前仍不能把 `/spin` 当作精确旋转原语。
- **复现/上下文**：首次重启后连续执行底盘 release 与 spin。向 `/spin` 发送目标 `0.5235987756 rad`（`30°`），随后读取 `/spin` 反馈、`/odom` 和 `odom -> base_link`。
- **确认事实与证据**：
  - 在组合执行 release 与 spin 之前读取的 odom 航向约为 `0°`。
  - 没有在 release 完成后、spin 开始前读取独立 odom 基线，因此无法从现有记录分别计算两个阶段的航向变化。
  - `/spin` 调用成功，末次反馈约 `0.5735 rad`（`32.9°`）。
  - 最终 `/odom` 和 `odom -> base_link` 航向均约 `1.009 rad`（`57.8°`），与用户现场观察的约 `60°` 一致。
  - 按组合序列前约 `0°` 的读数，约 `57.8°` 是 release 与 spin 整个组合序列的总航向变化；它与 `/spin` 末反馈约 `32.9°` 相减得到约 `24.9°` 的未解释残差。这是读数之间的观察，不能证明 `/spin` 本身不一致，也不能证明残差发生在 release、mode transition、spin 或其他阶段。
  - 在一次性低速、基于 `/odom` 的闭环右转修正中，航向从 `57.799°` 修正到 `30.307°`，后续静态读数约 `30.2°`。
  - 修正完成后 twist 各分量均为 `0`，`diffbot_base_controller` 为 active。
- **根因或假设**：永久根因未确认，现有证据不能定位约 `24.9°` 残差来自哪个阶段，也不能确认 `/spin` 本身存在偏差。首次重启后 release 与 spin 连续执行，release/mode transition 可能对该残差有贡献，但这仅是待复现的**假设**。需要在 fresh reboot 后单独测量 release 前后以及 spin 前后的 odom 才能判断。
- **处理**：经用户授权，在空旷处进行一次性低速 `/odom` 闭环右转，将航向修正回约 `30°`。在问题隔离前，不使用 `/spin` 作为精确原语；受控旋转测试使用 odom feedback。该方法是测试绕过方案，不是永久修复。
- **验证**：闭环修正后 odom 为 `30.307°`，随后静态约 `30.2°`；twist 全为 `0`，`diffbot_base_controller` 保持 active。
- **剩余工作/链接**：在 fresh reboot 的安全受控实验中，分别记录 release 前、release 后、spin 前、spin 反馈过程和 spin 后的 odom，首先验证 release/mode transition 是否引入航向变化；在完成隔离和重复性验证前，继续采用 odom feedback。当前状态见 [`CURRENT_STATUS.md`](CURRENT_STATUS.md#当前问题)。

## BOT-20260818-04

- **标题**：perception-only 入口未加载已有 gRPC 运行环境
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已解决
- **现象与影响**：直接运行 `test_book_perception.py` 时在创建 `BookVisionClient` 阶段报 `BookVisionError: grpc_not_installed`，尚未进入相机或 5090 推理。
- **复现/上下文**：机器人系统 Python 的默认 `sys.path` 不包含视觉运行包自带的 gRPC sysroot；项目配置中的默认证书和 protobuf 路径也不是当前机器人实际安装位置，三个视觉身份变量默认为空。
- **确认事实与证据**：
  - gRPC 已安装在 `/home/unix_ai/.local/share/bookbot/vision-rpc/runtime/sysroot/usr/lib/python3/dist-packages`，现有视觉 runner 会把该目录加入 `PYTHONPATH`。
  - 当前证书、客户端私钥和 protobuf 位于机器人已有的 vision-rpc 运行目录，不在项目配置的默认路径。
  - 临时加载 sysroot 并传入当前 listener 身份与实际文件路径后，`BookVisionClient` 可以连接 `127.0.0.1:7443`。
- **根因或假设**：确认根因是新的 perception-only 入口没有复用机器人既有视觉 runner 的 Python 和运行身份环境。
- **处理**：新增并部署 `scripts/book_vision_env.sh`，由 `run.sh --book-align` 加载现有 gRPC sysroot、protobuf、证书路径和当前视觉服务身份；没有把凭证内容写入 Git。
- **验证**：正式 `--book-align` 入口成功连接 7443 服务并在真实画面返回 5 本书，随后完成底盘对位与移动后复检。
- **剩余工作/链接**：当前无需继续处理；视觉服务身份或安装位置变化时同步更新环境脚本。当前状态见 [`CURRENT_STATUS.md`](CURRENT_STATUS.md)。

## BOT-20260818-05

- **标题**：关节历史缓存不足导致同步 RGB-D 快照无法形成
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已解决
- **现象与影响**：运行环境修正后，默认 pipeline 返回 `ok=false`；诊断入口在 5 秒内仍无法形成同步 RGB-D、CameraInfo 与关节快照，因此没有调用 5090。
- **复现/上下文**：五条输入 topic 均有发布者。同步器允许 RGB-D 和关节时间戳相对彩色帧各偏差 `100 ms`，但关节历史默认仅保留 64 条。
- **确认事实与证据**：
  - 同一窗口中，彩色帧到最近深度帧约 `72.9 ms`，CameraInfo 为 `0 ms`，均在 RGB-D 阈值内。
  - 彩色帧到缓存中最近的 `body_joint`、`joint_head0`、`joint_head1` 分别约 `828.3 ms`、`830.4 ms`、`830.4 ms`；图像对应时刻的关节样本已经被高频关节消息挤出 64 条缓存。
  - 只在单次进程中把 `joint_buffer_size` 从 64 扩大到 512 后，同一 pipeline 成功返回 `base_link=(1.082, 0.292, 0.740) m`。
  - 本次叠加图显示 5090 mask 覆盖画面下缘的绿色长方形目标，但目标部分贴近或超出画面下边缘。
  - 随后把头部从约 `0 rad` 平滑移动到现有标定低头姿态 `joint_head1=0.24997 rad`；新画面可见桌上 5 本书，pipeline 正确覆盖最左侧绿色书并返回 `base_link=(0.630, 0.262, 0.777) m`。
  - 将 FruitTest 从单书返回改为置信度阈值 `0.25`、最多 5 本后再次拍摄；5090 返回的 5 个候选均通过 Wanda 本地几何，置信度范围 `0.538–0.565`，同一张叠加图分别覆盖全部五本书。
- **根因或假设**：确认根因是默认关节历史保留时间短于实测图像消息到达延迟，不是相机 topic 缺失或 5090 无响应。
- **处理**：同步器永久使用 512 条关节历史，并将关节最大时间偏差调整为实测可用的 `250 ms`；对应回归测试已加入项目。
- **验证**：部署后真实 `--book-align` 在移动前后均形成同步快照，分别返回 5 本书，并成功完成对位闭环。
- **剩余工作/链接**：长短轴、机器人视角近端、右边缘和固定偏移诊断仍属于感知几何验收工作，但不再属于本缓存故障。当前状态见 [`CURRENT_STATUS.md`](CURRENT_STATUS.md)。

## BOT-20260818-06

- **标题**：头部 1080p 彩色图在 BEST_EFFORT 订阅下整帧丢失
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已解决
- **现象与影响**：`--book-align` 超时并返回 0 本；超时诊断显示 `color=0, depth=8, info=8`，因此没有调用 5090。
- **复现/上下文**：相机发布 1920×1080 彩色原始图。项目最初对彩色、深度和 CameraInfo 统一使用 ROS sensor-data 的 `BEST_EFFORT` QoS。
- **确认事实与证据**：
  - 故障时 `/head_rgbd/color/image_raw` 有一个 publisher，独立 `ros2 topic hz` 实测约 `5 Hz`。
  - 同一进程在 20 秒内收到 8 条深度、8 条 CameraInfo，但没有收到完整彩色消息。
  - 将 RGB-D 与 CameraInfo 订阅改为 `RELIABLE` 后，正式入口收到彩色帧；首个 RPC 失败后还能取得下一组新鲜帧并识别 5 本书。
- **根因或假设**：确认故障边界是大尺寸彩色消息在 `BEST_EFFORT` 订阅下没有完整送达当前进程。底层是否由 DDS 分片丢包、队列或其他传输细节造成未单独证明，不作更深归因。
- **处理**：RGB-D 与 CameraInfo 改用深度 2 的 `RELIABLE` QoS；关节订阅保持 sensor-data QoS。
- **验证**：部署后完整 `--book-align` 成功完成移动前检测、真实底盘对位、移动后检测并正常退出。
- **剩余工作/链接**：当前无需继续处理；若彩色帧再次消失，应先记录 publisher 频率和本进程缓存计数再重新打开本条。

## BOT-20260818-07

- **标题**：旧导航升降上限 `0.300 m` 与当前真机可达位置不一致
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已绕过
- **现象与影响**：首次对位在横向移动后进入升降步骤，要求从当前 `0.280 m` 上升到约 `0.288 m`；15 秒内位置保持 `0.280 m`，旧适配器以 `torso target was not reached` 退出，前后对位未执行。
- **复现/上下文**：`navnav_final` 的 `build_base_alignment_commands` 接受 `[0.0, 0.3] m`，按 `current + observed_z - reference_z` 生成绝对升降目标。
- **确认事实与证据**：
  - 故障时 `/lifting_controller/joint_states` 报告 `body_joint=0.280 m`、速度为零。
  - 所需高度增量约 8 mm，旧导航生成约 `0.288 m` 目标，但真机在超时前没有越过 `0.280 m`。
  - FPC 对位入口把升降目标裁到 `0.280 m` 后跳过不可执行的高度命令，横向和前后对位成功完成。
  - 移动后高度残差仍约 `+0.009 m`，与未执行上升一致。
- **根因或假设**：已确认直接原因是高度基准错误。旧公式 `current_torso + observed_z - reference_z` 隐含当前升降柱已位于录制起点；实际当前值为 `0.280 m`，而 `S1_TABLE_PICK_BOOK` 第 0 帧约为 `0.200 m`。`0.280 m` 是否也是控制器永久硬上限仍无权威文档，但它不再影响本次目标计算。
- **处理**：最初曾在 FPC 中改用 `0.200 m + observed_z - reference_z` 并验证实际 torso；随后确认 DataReplay 的逐帧绝对 `0.20 m` 会覆盖导航阶段预置，因此该修复被最终 XY/Z 分流设计取代。FPC 现在令导航 observed Z 恒等于 reference Z，完全不生成 torso 命令，并将初始 `book_z_delta` 作为固定 `z_offset_m` 交给旧 Pipeline。没有修改只读参考工程 `navnav_final`。
- **验证**：自动化测试证明选书只比较 X/Y、Z 不进入导航构建器、±30 mm 业务限幅和正/负/零 offset 均按合同输出；完整本地 55 项测试通过。FPC 导航侧不再需要真机升降验证。
- **剩余工作/链接**：旧 Pipeline 的 token、Pick/Place torso 逐帧偏移和物理 Z 精度属于后续集成，不再作为本条 FPC 导航故障。当前状态见 [`CURRENT_STATUS.md`](CURRENT_STATUS.md#正在进行)。

## BOT-20260818-08

- **标题**：右 D01 吸盘 Modbus 请求无回包
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：已解决
- **现象与影响**：`dual-d01-control` 服务在线，但右侧报告 `available=false`、`communication_ok=false`、`healthy=false` 和 `attachment_state=communication_lost`，因此当前不能测试吸附。
- **复现/上下文**：服务使用右侧 FTDI `AR8J81NT` 对应的 `/dev/ttyUSB1`；错误为 `ModbusTimeoutError: serial timeout after 0 of 3 expected bytes`。
- **确认事实与证据**：
  - FTDI 正常枚举，设备权限正常，服务独占串口且文件描述符指向正确设备。
  - 服务能写出请求，但地址 `1/9` 及全部六个受支持波特率组合均收到 0 字节。
  - 未占用的另一侧 FTDI 也没有 D01 回包；一次干净服务重启没有改变结果。
  - 排查完成后 `dual-d01-control.service` 已恢复 active/running；未发送吸附 start、控制寄存器写入或机器人运动命令。
- **根因或假设**：软件侧证据把故障边界缩小到 FTDI 转换器下游。D01 主电源未上电、RS485 A/B/地线或插头断开均与现象一致；远程软件检查不能在这些物理原因之间唯一判定。
- **处理**：用户完成现场处理，D01 服务自动恢复与右吸盘通信。
- **验证**：只读 `client.py status --side right` 返回 `available=true`、`communication_ok=true`、`healthy=true`、`attachment_state=idle` 和 `last_error=null`。未发送吸附 start。
- **剩余工作/链接**：通信故障已关闭；真实吸附/释放测试由机械臂与吸盘工作线安排。

## BOT-20260818-09

- **标题**：离散底盘 XY 对位不能达到 4 mm 精度
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-18（Asia/Shanghai）
- **状态**：待解决
- **现象与影响**：XY/Z 分流后的第一次真机测试执行成功，但一次离散对位后仍有 `dx=+0.008 m, dy=+0.023 m`，不能作为 4 mm 抓取对位结果。
- **复现/上下文**：机器人静止检测的初始残差为 `dx=-0.012 m, dy=-0.025 m`；旧 `navnav_final` 通过转向、横移等效动作、转回和前后移动执行 4 条命令，再拍摄一次验收图。
- **确认事实与证据**：
  - 五本书在移动前后均成功检测，固定目标的视觉 Z offset 都约为 `+0.011 m`。
  - 导航只收到 reference Z，没有执行 torso 命令；结束时 `body_joint=0.280 m`。
  - 结束时 odom 的线速度和角速度均为零，不是尚未停稳时读取的残差。
  - 本轮没有运行机械臂、DataReplay 或吸盘。
- **追加调查**：用户肉眼未观察到机器人移动，而旧测试没有动作前 odom 基线。随后静止 5 次感知中 4 次有效，同一目标 X/Y 跨度约 `1.5 mm/1.0 mm`，第 5 次明确无检测；因此静止视觉波动不能解释约 48 mm 的 Y 变化，但实际底盘位移仍需用下一次起终 odom 直接确认。
- **vector 真机证据**：初始残差 `dx=+0.009 m, dy=+0.024 m, dz=+0.012 m`；3 条 vector X/Y 命令产生 task-origin odom `dx=+0.014 m, dy=+0.034 m, yaw=+0.027 rad`。停稳后的视觉残差为 `dx=-0.008 m, dy=-0.045 m, dz=+0.010 m`，因此 X 在 10 mm 内但 Y 未达标。最终 odom twist 全零，`body_joint=0.280 m` 且速度为零；前后两次均检测到五本书。
- **根因或假设**：vector 证据已排除“命令只计算、底盘完全未动”，并证明当前实现没有改善 Y 精度。可能涉及 base_link 误差到 task-frame 运动的坐标变换、旋转期间的位移投影或直线段过冲；现有一次试验不能在这些原因之间唯一判定。
- **处理**：新增可选 `vector` 模式，将 dx/dy 合成为一次最短转向的前进或倒车位移；原 `legacy` 模式仍为默认。两种模式均记录 task-origin 后的 odom `dx/dy` 与 IMU yaw。该最小改进仍不是 20–50 Hz 连续 SE(2) 控制器。
- **验证**：首次 vector 真机测试已保存初始/最终 X/Y、task-origin odom `dx/dy/yaw` 和停止状态，结论为未通过。4 mm 成功标准仍需按独立测试设计重复验证。
- **剩余工作/链接**：先核对 vector 坐标变换、旋转后的实时航向和每段运动反馈，再确定连续微对位与机械臂残差补偿的职责边界。当前状态见 [`CURRENT_STATUS.md`](CURRENT_STATUS.md#正在进行)。

## BOT-20260818-10

- **标题**：Stage 1 Pick 没有完整复现录制的起始状态与底盘轨迹
- **首次发现**：2026-08-18（Asia/Shanghai）
- **最近更新**：2026-08-19（Asia/Shanghai）
- **状态**：代码已部署，真机部分验证
- **现象与影响**：第一次完整 Pick 尝试在程序内已接近旧参考点，但用户现场确认机器人仍离桌过远。DataReplay 到达第 300 帧后右 D01 未形成真空，以 `suction_timeout` 停止，没有抓起书。
- **复现/上下文**：初始选中吸取点约 `(0.903,-0.358,0.766) m`，旧参考为 `(0.9116,-0.3151,0.7552) m`；移动后约 `(0.909,-0.331,0.766) m`，所以程序只做了厘米级运动。torso 已按 `+0.011 m` 偏移预置到约 `0.211 m`，回放在第 300 帧 D01 事件处终止。
- **确认事实与证据**：只读检查真实 Pick HDF5 发现 607 帧 `target_base_vel` 在多段非零；按录制时间戳积分，到第 300 帧前线速度命令累计约前进 `0.347 m`。旧 V3 配置却设置 `allow_base_motion=false`，FPC 旧桥也明确要求底盘禁用，因此所谓 DataReplay 实际只回放了手臂/头部/升降柱，漏掉了录制中用于接近桌面的底盘运动。旧入口还只预置 torso，没有从反馈恢复第 0 帧双臂和头部姿态；D01 事件在旧发布器中发生于第 300 帧发布之前。上述都是代码和资产可直接复现的事实。
- **根因或假设**：确认根因是把含底盘运动的录制资产按“底盘禁用”合同执行，同时没有建立完整的第 0 帧全身姿态合同。此前把现象归因于 `0.9116 m` 参考点并改用 `0.48 m`，只处理了表面距离，不能重建录制轨迹；该判断现已更正。视觉参考仍应来自录制第 0 帧的同一套 13 cm/10 cm 书本几何。
- **处理**：默认从当前位置恢复第 0 帧几何，`0.48 m` 仅保留为显式 `--book-coarse` 粗定位；最终 X/Y 超差时禁止回放。Pick 桥强制并验证六个录制动作通道，用统一有界轨迹恢复第 0 帧双臂/头部/torso 姿态，然后发布原始 607 帧及底盘速度。旧 navnav `START_TASK` 会额外校正绝对航向，因此改用不产生运动的本地 completion tracker。D01 在第 300 帧发布后异步触发，等待 holding 不阻塞后续帧；失败清理不再自动停止可能已吸住或状态未知的吸盘。
- **验证**：commit `ccd0699` 已部署。真机已完成第 0 帧对位、姿态预置、录制底盘速度发布并在吸附阶段实际吸起书，证明旧的“漏掉底盘轨迹”和“未恢复第 0 帧姿态”问题已越过原失败点；但软件返回 `D01 event failed/faulted/stale` 并退出，尚未取得全 607 帧正常完成和 holding ACK 证据。
- **剩余工作/链接**：在处理吸附点偏心的下一次实验中，同时保存 frame 0/300/606 计数与 D01 ACK；未取得这些证据前本条不能标记为完全解决。当前状态见 [`CURRENT_STATUS.md`](CURRENT_STATUS.md#当前问题)。

## BOT-20260819-11

- **标题**：吸附阶段成功但吸附点相对书本中心偏移
- **首次发现**：2026-08-19（Asia/Shanghai）
- **最近更新**：2026-08-19（Asia/Shanghai）
- **状态**：待解决
- **现象与影响**：Stage 1 回放到吸附阶段后已实际吸起 `FOLIO-02`，Z 正确；软件仍返回 `D01 event failed/faulted/stale`，不能声称 607 帧正常完成。用户现场测得实际吸附点相对书本真实中心 X 约偏 `50 mm`、Y 约偏 `10–20 mm`，肉眼方向为偏左。
- **复现/上下文**：抓取前最终视觉残差为 `dx=-8 mm, dy=-9 mm, dz=+16 mm`。当前视觉目标使用从近端向内 `130 mm`、从右边缘向左 `100 mm` 的固定点，并非书本中心。
- **确认事实与证据**：录制参考书尺寸为 `257.660 × 174.036 mm`，其 13/10 点相对中心约为 `base_link XY=(+1.7,+12.9) mm`；叠加本次残差预计约为 `(+9.7,+21.9) mm`，足以解释 Y 而不足以解释 X。当前参考 JSON 只保存第 0 帧视觉规则点，不含真实吸盘 TCP/接触点。到 frame 300 的底盘速度积分约为 `+348.9 mm`，但 HDF5 `base_pose` 全零，回放内部没有 odom 闭环；14% 位移差即可产生 50 mm X 误差。CameraInfo 内参与录制脚本完全一致，头部角一致，畸变差分与静止视觉噪声均远小于 50 mm。
- **根因或假设**：Y 偏心主要由“固定 10 cm 点不是中心”及本次 9 mm 最终残差组成。X 尚未唯一归因，优先怀疑录制真实接触点未标定和约 349 mm 开环底盘接近的实际位移差；不同书长导致 13 cm 点相对中心变化也是贡献项。
- **处理**：本轮只完成只读诊断和记录，未修改算法、未恢复尺寸/朝向门禁、未再次发送运动或 D01 命令。
- **验证**：下一次保存平放书中心与 13/10 点、frame 0/300/606 odom，以及带方向的实际接触偏移。完整分析见 [`GRASP_POINT_OFFSET_ANALYSIS_20260819.md`](GRASP_POINT_OFFSET_ANALYSIS_20260819.md)。
- **剩余工作/链接**：依据上述三组数据决定改为几何中心目标、资产固定接触偏移，或闭环复现 DataReplay 底盘位移；不能通过猜测相机标定常量修正。

## BOT-20260820-12

- **标题**：推车粗导航在旋转后重复交换 X/Y，错误驶向书桌
- **首次发现**：2026-08-20（Asia/Shanghai）
- **最近更新**：2026-08-20（Asia/Shanghai）
- **状态**：已解决
- **现象与影响**：`--cart-approach-navigation` 完成六角度扫描后转向书桌并连续前进，用户现场确认机器人撞到桌子；第四个直行 action 随后以状态 6 中止。
- **复现/上下文**：本任务的坐标始终固定为机器人最初面朝书本时的坐标：X 为前后、Y 为左右。错误实现却在机器人已左转 90° 后将原点目标再次转换到当前 body frame，并把转换后的 Y 当作原始左右位移，再额外旋转 90°执行。
- **确认事实与证据**：Nav2 behavior 日志显示前三个 `drive_on_heading` 成功，第四个报告 `Collision Ahead` 并 abort；异常栈位于粗定位横向命令循环，尚未进入 0.8 m 前后定位、二次视觉微调或 Place。测试进程退出后没有残留 `--cart-approach-navigation` 进程。
- **根因或假设**：确认根因是代码把“为了差速底盘执行 Y 而临时旋转”错误当成坐标系也应旋转，导致 X/Y 重复交换。0.8 m 距离当时尚未用于发生碰撞的运动段。
- **处理**：检测点只转换一次到扫描起点/书本坐标系；左转 90°后直接按 `target_y-current_y` 行驶，右转 90°恢复原航向。由于流程确定从 DataReplay 结束位置先后退 `0.20 m`，恢复航向后只固定前进同样的 `0.20 m`，不再用推车深度估算粗定位 X；随后由正面新 RGB-D 帧完成 0.8 m X/Y 微调。移除旋转后 body-frame 横向公式，并在动作前打印计划位移。
- **验证**：离线测试明确验证命令顺序为 Y 直行、右转 90°、X 直行，且二次 vector 微调仍在恢复后的书本坐标系中运行。尚未重新发送真机运动命令。
- **剩余工作/链接**：部署后由用户重新布置并确认现场安全，再单独运行 `--cart-approach-navigation`；保存实际打印的 `move_y/move_x` 与现场方向后才能关闭本条。
- **2026-08-20 更正**：现场已确认修正后的书本坐标系长距离方向正确。随后暴露的碰撞来自恢复航向后的旧 `0.8 m` 整车中心二次精调，不属于本条 X/Y 重复交换根因。当前源码已彻底删除 `0.8 m` 精调、地图固定点和断点续跑；粗导航在固定前进 `0.20 m` 后结束，后续由 Place 第 0 帧托板几何精定位接管。
